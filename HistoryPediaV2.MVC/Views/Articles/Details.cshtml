@using System.Net
@model HistoryPediaV2.Data.Models.Article
@{
    ViewBag.Title = Model.Name;
}


<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="page-header">
                <h1>
                    @Model.Name
                </h1>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-9">
            <p class="text-left">
                @Model.Info
            </p>
        </div>
        <div class="col-md-3" style="margin: 0 auto;">
            @if (!string.IsNullOrEmpty(Model.Image?.Path))
            {
                <img class="card-img-top" alt="Bootstrap Thumbnail First" src="@Model.Image.Path">
            }
            else if(Model.Image?.Image is not null)
            {
                <img class="card-img-top" alt="Bootstrap Thumbnail First" src="data:image/jpeg;base64,@(Convert.ToBase64String(@Model.Image.Image))">
            }
            else
            {
                <img class="card-img-top" alt="Bootstrap Thumbnail First" src="\images\Def1.jpg">
            }
        </div>
    </div>

    @foreach (var item in Model.Blocks)
    {
        <div class="row">
            <hr class="my-4"/>
            <div class="row">
                <div class="col-md-12">
                    <h2>
                        @item.BlockName
                    </h2>
                    <p>
                        @item.Text
                    </p>
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                </div>
                <div class="col-md-4">
                    @if (string.IsNullOrEmpty(item.Image.Path))
                    {
                        <img class="img-thumbnail shadow-sm" alt="Bootstrap Image Preview" src="data:image/jpeg;base64,@(Convert.ToBase64String(@item.Image.Image))">
                    }
                    else
                    {
                        <img alt="Bootstrap Image Preview" src="@item.Image.Path" class="img-thumbnail shadow-sm">
                    }
                    <p class="text-center">
                        @item.Image.PictureText
                    </p>
                </div>
                <div class="col-md-4">
                </div>
            </div>
        </div>
    }

    <div class="row">
        <div class="col-md-2">
            @*@if (Model.UserId == null)
            {*@
                <a type="button" class="btn btn-block btn-outline-info shadow" asp-action="AddToFavorite" asp-controller="User" asp-route-id="@Model.ArticleId">
                    Add to favorite
                </a>
            @*}
            else
            {
                <a type="button" class="btn btn-block btn-outline-danger" asp-action="RemoveFromFavorite" asp-controller="Favorite" asp-route-id="@Model.Id">
                    Remove from favorite
                </a>
            }*@
        </div>
        <div class="col-md-8">
        </div>
        <div class="col-md-2">
            <a type="button" class="btn btn-block btn-outline-warning shadow mx-2" asp-action="Edit" asp-controller="Articles" asp-route-id="@Model.ArticleId">
                Edit
            </a>
            <a type="button" class="btn btn-block btn-outline-danger shadow" asp-action="Delete" asp-controller="Articles" asp-route-id="@Model.ArticleId">
                Delete
            </a>
        </div>
    </div>
</div>
